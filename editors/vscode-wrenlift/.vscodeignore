.vscode/**
.vscode-test/**
src/**
sample/**
.gitignore
.github/**
**/tsconfig.json
**/.eslintrc.json
**/*.map
**/*.ts
.git/**

# Keep production node_modules in the .vsix — the extension requires
# `vscode-languageclient/node` at runtime. CI prunes dev deps with
# `npm prune --omit=dev` before `vsce publish`, so only the LSP
# client and its transitives ship.
