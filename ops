{"author":{"id":"694fb8161bd8bf91df2a28db7c649e48f8c5fa47ad3ad0234607d35f0482801a"},"ops":[{"type":3,"timestamp":1790280314,"nonce":"JkJyrewetcrEE1fWb4SIH6pTwME=","message":"State at bb2542c. Piece 1's core landed in d8bb4bb: liveness over the MIR finds the boxed values live across every helper that may collect, and the emitted body pushes them on the runtime's root store around it, restoring to the depth taken at entry (scan_roots_across in src/codegen/wasm.rs). What piece 1 still names is not done: wasm/src/lib.rs parks fibers as a bare *mut ObjFiber in a static list, so a collection while one is parked can sweep it (VM::handles are traced, so a handle taken at park and released at resume would root it); tier_wasm's last_module_vars_cache keys on a closure pointer and would answer for a freed closure whose address was reused; current_closure_cell and wasm/src/tier_up.rs's main_module_cache hold raw pointers whose lifetime rests on the caller's frame and a per-run reset. Pieces 2 (Immix on wasm32) and 3 (fibers for JIT'd bodies) are untouched. The browser target has no runtime coverage in the gate — only cargo check — so these want the wasm test arm of the issue before they are written.","files":null}]}