{"author":{"id":"694fb8161bd8bf91df2a28db7c649e48f8c5fa47ad3ad0234607d35f0482801a"},"ops":[{"type":1,"timestamp":1789857283,"nonce":"CG6WBXBi0tbC8lexgyKUEJPdupw=","title":"Tiered run of caribou's swarm arena ends in \"Subscript must be an integer\" after a deopt of run(_,_)","message":"Reproducer: caribou's fixture crates/caribou-interop/fixtures/src/swarm/arena.wren (pure Wren, no bridge) with a two-line driver beside it:\n\n    import \"arena\" for Arena\n    System.print(Arena.run(1000, 60))\n\n`wlift --mode tiered drive.wren` (0.1.22, main 1d8f09a, and 0.1.21 c533f4c the same) fails with `Subscript must be an integer.` attributed to `return w.checksum() + acc + score + hud` (arena:258); `--mode interpreter` prints -36666.278525854. Sizes: (1000, 1), (1000, 5), (300, 20), (200, 60) pass; (500, 60) and (1000, 60) fail. WLIFT_TIER_TRACE=1 shows `deopt FuncId(22) at pc=400 live=42` and `speculation failed FuncId(22), recompiling` for run(_,_) shortly before the error, after update(_), step(_) and run(_,_) were installed Optimized; a live value restored by that deopt is presumably no longer integral (a loop counter or a list index), and the subscript that then fails sits in inlined code, so the location names the caller.\n\nThe bench worked at 032a722 (caribou pinned it until today), so the regression is in 032a722..c533f4c; the deopt and inline-list-read commits in that range are the suspects. Caribou's Bench workflow (swarm --mode wren) is red on it.","files":null}]}