{"author":{"id":"694fb8161bd8bf91df2a28db7c649e48f8c5fa47ad3ad0234607d35f0482801a"},"ops":[{"type":3,"timestamp":1789846913,"nonce":"1MvlY5uNxfeTGEVhegyXiBLCi1I=","message":"8857ce0: the last structural lever. ObjList gained elem_class at offset 40 (LIST_SIZE 48): 0 unseen, a class pointer while every element written was an object of that class, ELEM_CLASS_MIXED otherwise; kept by ObjList::set/add/insert (clear resets, as_mut_slice marks mixed) and by the compiled inline writers — Cranelift's inlined add, the LLVM tier's inlined add, subscript set and list literal — with every other path (core list natives, plugin API, make_list helpers) going through those methods; the audit found no raw element writer outside ObjList. At the top tier a SubscriptGet whose result a GuardClassAt follows (found by scanning the block, through copies) answers the guard on the List path from elem_class == C — a list-header-tagged load a loop over one list hoists and unswitches — and elsewhere from the element's class, phi'd into the guard. In advance() b's check became a testb on a hoisted bool. Found on the way and fixed in the same commit: a guard failing in a module body errored with 'found no closure to resume' (present at 55f71b6): the module body ran with no closure and method_binding[0] was null; it is now bound to a closure at module run and the bound closures are collector roots (a deopt under a 1 MB heap with 14 collections resumes correctly). NUC at load 4: ash/nbody 0.697, bench/nbody 0.093; a quiet run is owed.","files":null}]}