{"author":{"id":"694fb8161bd8bf91df2a28db7c649e48f8c5fa47ad3ad0234607d35f0482801a"},"ops":[{"type":3,"timestamp":1789296247,"nonce":"jJ99mIUuklFNshMK3xDkh6hQuBU=","message":"87224d3 .. cd323fa — binary trees and dispatch. Profiling bench/ash/binary_trees showed the compiled code at 9% and the rest in the constructor dispatch chain: TreeNode.new went wren_call_3 -\u003e dispatch_call -\u003e dispatch_method -\u003e call_constructor_sync, which hashed the module name (SipHash) and zeroed a 16-slot arg array per call, then ran the initialiser through call_jit_fn; itemCheck went through wren_known_call_nocheck (TLS fetch, root copies, module lookup); _left == null called wren_cmp_eq. Landed: constructor sites resolved at compile time (kind 3 caches from the module variable's class) call wren_alloc_instance and the compiled initialiser directly; known calls to same-module callees without static fields/upvalues call straight through jit_code with a process-wide depth word (helper past MAX_JIT_DEPTH); equality on non-Num operands compares bits inline unless the class carries CLASS_FLAG_EQ (user ==/!=, String); known calls no longer copy args to the root set under Immix; dyn_dispatch mirror of Ash's BenchDynDispatch added (checksum 1148967109). Also found via Guard Malloc a heap overflow from the baseline result profile writing past the register file (d4532dc-adjacent issue, closed) that had been corrupting the compile threads intermittently.\n\nNUC: ash/binary_trees 2.5 s -\u003e 1.0 s (Ash AOT 0.31 s on Linux CI, 0.36 s hybrid on the Mac); bench/binary_trees (CI's, depth 14) 0.58 -\u003e 0.26 s; ash/dyn_dispatch 0.30 off / 0.23 llvm (Ash hybrid 0.31 s, jit 0.05 s on the Mac); nbody 1.07 llvm; delta_blue off 0.09.\n\nWhat binary_trees spends now (Mac profile): compiled code 30%, allocation path 30% (wren_alloc_instance + finish_alloc + should_collect + alloc_raw + a TLS fetch each), collector 25% (collect_end, for_each_child, object_drop, drop_in_place). No dispatch helper remains on the path; type lookups are all cache hits (class byte, IC class check, jit_code slot). The rest of the gap to Ash is the allocator/collector workstream on 6923f6e: an inline bump allocation and a sweep that does not drop objects one by one.","files":null}]}