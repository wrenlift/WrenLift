{"author":{"id":"694fb8161bd8bf91df2a28db7c649e48f8c5fa47ad3ad0234607d35f0482801a"},"ops":[{"type":3,"timestamp":1789914615,"nonce":"rn7XIVSfBH6TGRxs8vAXvhKWe0o=","message":"First step in 28c5853a0c146478a21b697bac4c845663e7786b: reads. LICM hoists GetModuleVar out of a loop that never writes the slot and runs no user code (may_run_code: calls, toString/concat, subscripts, boxed operators on a receiver not known Num), and the OSR entry rematerialises such a preheader read (osr_rematerializable_defs) under the same conditions, since both the interpreter and native entries would otherwise need a register for it. A second, reads-only LICM run after unboxing catches the loops whose compare became typed. Measured nothing on the NUC (method_call's first loop loses ten loads a trip, but its 20 ms are compile latency); the numeric module-level loop case (7 ns/iter vs 1 ns in a method) needs the written-slot half: loop-carried block parameters for stored slots, store-back on every exit, deopt live entries that write module slots (a DEOPT_MODVAR tag, resume_method_sync writing the module var), a store before the opt tier's retier poll, and an OSR live-in kind that reads a slot. Without a Num guard on the loaded value the arithmetic stays boxed, so that half also needs a guard at the loop entry with a resume point at the header. Not started.","files":null}]}