{"author":{"id":"694fb8161bd8bf91df2a28db7c649e48f8c5fa47ad3ad0234607d35f0482801a"},"ops":[{"type":3,"timestamp":1789834366,"nonce":"UFmGax4RnX+PHsgutNq3WCmycGI=","message":"cac3b73: the in-place class guard reads the class branchlessly (class_of), so the load runs on every path; LLVM still keeps it in the loop for the invariant receiver because the earlier subscript exits mean it is not guaranteed to execute and an inttoptr address is not provably dereferenceable, so the gain is one branch. Next concrete lever in advance()'s inner loop, per the LLVM asm: the list subscript costs ~16 instructions per iteration — the integrality test on the f64 counter j (fcvtzs/scvtf/fcmp), a reload of the list's type byte, count and elements pointer, and the storage-kind switch. The counters stay f64 because int_loop (src/mir/opt/int_loop.rs) cannot bound size = _bodies.count: the call result is Top. A List.count devirtualised to its own instruction (devirt.rs already recognises the site and leaves it as a Call) with a Num range of 0..2^32 would bound i and j, carry them as i64, and let the subscript skip the integrality test; the type-byte load can be an invariant.load, and count/elements loads with a TBAA type disjoint from instance fields could be hoisted once the loop has no exits before them.","files":null}]}