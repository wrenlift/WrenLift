{"author":{"id":"694fb8161bd8bf91df2a28db7c649e48f8c5fa47ad3ad0234607d35f0482801a"},"ops":[{"type":1,"timestamp":1789210736,"nonce":"j2qxY/Sw1GgwgGMN87990ffqjy8=","title":"fiber.try() did not resume an inner fiber across an outer try() boundary","message":"Closed 2026-09-12: no longer reproduces at 5792bac (interpreter and tiered). Fixing commit not identified; repro kept below in case it regresses.\n\n\n```wren\nvar outer = Fiber.new {\n  var count = [0]\n  var inner = Fiber.new {\n    count[0] = count[0] + 1\n    Fiber.yield()\n    count[0] = count[0] + 10\n  }\n  inner.try()            // count = 1\n  System.print(count[0]) // 1  (OK)\n  inner.try()            // should resume after Fiber.yield → count = 11\n  System.print(count[0]) // 1  (WRONG — should be 11)\n}\nouter.try()\n```\n\nTop-level (no outer fiber) the same pattern works correctly —\n`inner.try()` resumes past the yield on the second call.\n\n\nThe `@hatch:web` Scheduler_ spawns per-connection fibers and\ndrives them with `fiber.try()`. The server's `listen()` runs at\ntop level so it works fine. But Test.run() wraps every test body\nin a fiber, so unit tests that verify scheduler + channel\n`receive` semantics can't — the inner receiver fiber yields once\nand never wakes on the test's next `s.tick`.\n\n\n- `live.spec.wren` tests Channel via `deliver_` (bypasses\n  receive's yield loop) and pre-fills the queue before calling\n  `receive`, so the yield path is never taken.\n- End-to-end fiber concurrency is exercised by the chat demo.\n- The server's own accept loop is unaffected.\n\n\n- `src/runtime/fiber.rs` — fiber state + parent-caller tracking\n- `src/runtime/vm_interp.rs` — Fiber.try / Fiber.yield\n  dispatch\n- Suspect: parent-fiber resumption resets the inner fiber's\n  frame pointer or loses its suspended PC when the outer fiber\n  is re-entered. Likely a single-field bug in the transfer path.\n\nStandard Wren passes this test in the reference implementation,\nso the behaviour is a wren_lift regression.","files":null}]}