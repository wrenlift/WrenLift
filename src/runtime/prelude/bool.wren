//! Built-in `Bool` class — only two instances, the literals
//! `true` and `false`. The class itself can't be instantiated.

class Bool {
  /// Logical NOT.
  ! {}

  /// `"true"` or `"false"`.
  toString {}
}
